---
import PressureMap from "./PressureMap.svelte";

const {
  variant = "hero",
  class: className = "",
  decorative = true,
  alt = "Synthetische thermische kaart van Nederland",
} = Astro.props;
---

<PressureMap
  variant={variant}
  className={className}
  decorative={decorative}
  alt={alt}
  client:visible
/>
